<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mukangulol.application3">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@drawable/sports_score_betway"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@drawable/sports_score_betway"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".controllers.activity.TeamDetailsActivity"></activity>
        <activity android:name=".controllers.activity.GamesActivity" />
        <activity android:name=".controllers.activity.LeaguesActivity" />
        <activity android:name=".controllers.activity.SchedulesActivity" />
        <activity android:name=".controllers.activity.TeamsActivity" />
        <activity android:name=".controllers.activity.MainActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>